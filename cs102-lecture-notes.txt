git clone git@github.com:Rayan-Scammander/CS102-git-essentials.git  (clone repo to local machine)

git add .  (add the changes to the stage)

git status (check the repo status)

git commit -m "local: added cs102 lecture notes" (Commit the changes to repo on local)

git push origin master (upload changes to cloud)
